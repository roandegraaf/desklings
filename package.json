{
  "name": "schermes",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.15.1",
  "engines": { "node": ">=24" },
  "scripts": {
    "check": "pnpm -r --no-bail check",
    "test": "pnpm -r --no-bail test",
    "dev": "pnpm --filter @schermes/daemon dev",
    "migrations": "pnpm --filter @schermes/daemon migrations"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^7.0.2"
  }
}
